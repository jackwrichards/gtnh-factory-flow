# ForgeGradle 2.x needs a comfortable heap to decompile the 1.7.10 Minecraft source.
org.gradle.jvmargs=-Xmx2G

# Keep the wrapper at a Gradle version the old ForgeGradle 2.1 actually ran on.
org.gradle.daemon=false
